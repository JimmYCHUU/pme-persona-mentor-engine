/**
 * StepRoadmap — visual roadmap of incomplete tasks. Stub for Phase 1.
 */

export function StepRoadmap() {
    return (
        <div style={{ padding: '16px', color: 'var(--text-muted)', fontSize: '0.85rem' }}>
            <p style={{ fontStyle: 'italic' }}>Roadmap will appear when project tasks are defined.</p>
        </div>
    )
}
