/** MasteryMap — mastery ledger sidebar view. Stub. */
export function MasteryMap() {
    return (
        <div style={{ padding: '16px', color: 'var(--text-muted)', fontSize: '0.85rem' }}>
            <h3 style={{ fontFamily: 'var(--font-heading)', color: 'var(--text-heading)', fontSize: '1rem', marginBottom: '8px' }}>Mastery Map</h3>
            <p style={{ fontStyle: 'italic' }}>No concepts tracked yet.</p>
        </div>
    )
}
