/** SliderPanel — inline slider controls for persona. Stub. */
export function SliderPanel() {
    return <div style={{ padding: '8px', color: 'var(--text-muted)', fontSize: '0.8rem' }}>Sliders</div>
}
